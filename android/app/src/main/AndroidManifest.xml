<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.littlewitchadventure"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="Little Witch Adventure"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >

        <!-- ID ứng dụng AdMob chạy thử nghiệm mẫu từ Google -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Little Witch Adventure"
            android:screenOrientation="sensorLandscape"
            android:exported="true" >
            
            <!-- Trùng khớp với Tên thư viện C compile từ NDK: liblittlewitch.so -->
            <meta-data android:name="android.app.lib_name" android:value="littlewitch" />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
